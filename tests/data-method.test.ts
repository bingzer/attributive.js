// ------------------------------------------------- //

describe('[data-method] constants and global vars', () => {
    it('Should have Attv global variables', () => {
        expect(Attv.DataMethod).toBeDefined();
        expect(Attv.DataMethod.Key).toBeDefined();
    });
});